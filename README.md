📱 Phone Directory — Almabase Assignment
🧩 Overview

This is a Phone Directory web application built as part of the Almabase assignment.
The project allows users to manage contact information efficiently — including adding new contacts, editing existing ones, deleting unwanted entries, and searching through the directory in real time.

The primary goal was to demonstrate a clean, maintainable, and functional implementation of CRUD operations, focusing on both usability and code structure.

🚀 Features

➕ Add Contact — Add new contact details (name, phone number, email, etc.)

🗂️ View Contacts — See all contacts in a clean, tabular format

✏️ Edit Contact — Update existing entries instantly

❌ Delete Contact — Remove contacts from the directory

🔍 Search Functionality — Quickly find any contact by name or phone number

💾 Persistent Data — Contacts are saved and retrieved from the backend

🎨 Responsive UI — Works smoothly across devices

🛠️ Tech Stack

Frontend: React.js / HTML / CSS / JavaScript
Backend: Node.js + Express (or Flask, if that’s what you used — please confirm)
Database: MongoDB / JSON server / LocalStorage (depending on implementation)
Version Control: Git & GitHub



⚙️ Setup & Installation
1️⃣ Clone the repository
git clone https://github.com/cold2511/almabase.git
cd phone-directory

pip install -r requirements.txt


run the application
python app.py


open in browser
http://127.0.0.1:5000   
